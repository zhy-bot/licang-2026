# 项目协作规则

本文件适用于整个 `chassis_motor` 工程。开始任何任务前，先按顺序完整读取：

1. `PROJECT.md`：项目结构、硬件关系、长期偏好和既有决策。
2. `REQUIREMENTS.md`：当前功能需求、接口约束和验收标准。
3. `docs/worklogs/LATEST.md`：最近一次工作状态、遗留问题和下一步建议。
4. 与本次任务直接相关的源文件、构建配置和说明书；不得脱离现有实现从零猜测。

## 执行原则

- 默认使用中文沟通，先给结论，再说明关键依据。
- 修改前确认现有模块边界，复用已有接口，不重复创建功能相同的模块。
- `main.c` 只负责系统初始化和顶层调用，电机、麦轮解算、运动控制、IMU 分别放在已有模块中。
- STM32CubeMX 生成文件尽量只修改 `USER CODE` 区；如必须修改生成区，应明确说明原因。
- 每次代码修改后至少执行一次 `.vscode/build.ps1`，报告实际错误和警告数。
- 除“能编译”外，还要主动检查方向、单位、超时、通信失败、自动启动、部分电机动作等隐藏风险。
- 保留用户已有改动，不覆盖与当前任务无关的内容。
- 外部参考用于交叉验证设计；最终实现必须结合本项目电机编号、安装方向、驱动协议和主控资源。

## 文档同步

- 硬件映射、模块结构或长期决策改变时，同步更新 `PROJECT.md`。
- 功能目标、动作顺序或验收条件改变时，同步更新 `REQUIREMENTS.md`。
- 用户明确说“结束今天工作”或同义表达时：
  1. 创建或更新 `docs/worklogs/YYYY-MM-DD.md`。
  2. 记录当天需求、修改文件、关键参数、验证结果、未解决风险和下次入口。
  3. 将 `docs/worklogs/LATEST.md` 更新为该记录的精简副本，并链接到当天文件。
- 新一天开始工作时，必须先读取 `docs/worklogs/LATEST.md`，再继续实施。

